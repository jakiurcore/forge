# Work Queue Backpressure

Experiment demonstrating bounded queue behavior with a faster producer than consumer.

Run with:

```bash
cargo run --example work_queue_demo -p forge-concurrency
```

Results and interpretation are recorded in `benchmarks/concurrency/queue-backpressure.md`.
