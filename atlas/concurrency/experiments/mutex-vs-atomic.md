# Mutex vs Atomic Counter

Benchmark comparing `SharedCounter` (Mutex) and `AtomicCounter` under contention.

Results are recorded in `benchmarks/concurrency/mutex-vs-atomic.md` after running the benchmark.
